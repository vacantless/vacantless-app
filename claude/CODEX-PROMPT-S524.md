Vacantless-app — build ticket S524 (Feature A). App HEAD `4c962b9` on main.

Read these files already in the working tree (do NOT ask me to attach anything):
- claude/CODEX-BUILD-CATEGORIZATION-SEED-A-S524.md — the authoritative build ticket (review steps, build spec, migration 0166, invariants, verification).
- claude/DESIGN-ACCOUNTING-CATEGORIZATION-SEEDING-S519.md — the why (§1, the FreshBooks-seed "leave FreshBooks" unlock).
- claude/DESIGN-PREMIUM-ACCOUNTING-INHOUSE-S518.md — the Premium accounting strategy this completes.

One-line task: let a Premium landlord upload their already-categorized transaction history from prior accounting (FreshBooks CSV first), MATCH those rows to the org's existing bank_transactions, and APPLY the prior categorization — via derived categorization rules (preferred, reusing lib/categorization-rules.ts + autoApplyRules) or direct filing — behind a review-before-commit preview. Rows the source marked personal/non-rental are EXCLUDED, never filed. Concretely: additive migration 0166 adds two org-scoped staging tables (categorization_import_batches + categorization_import_rows); new pure modules lib/accounting-import/{freshbooks,match,category-map,index}.ts (parser + exact-amount/date-window/fuzzy-desc matcher + source→Vacantless disposition map + plan builder); new Premium-gated surface app/dashboard/money/import-history/{page,actions}.ts (stage → preview → commit → discard); and scripts/test-accounting-import.ts.

Follow the ticket exactly. Constraints: reuse the EXISTING `accounting` Premium gate (no new gate key); no new external vendor / no network calls (reads only the org's own bank_transactions); NO Growth takeaway (net-new Premium surface); NEVER an unattended write to financial records (parse → stage → preview → operator confirm → commit); Feature A does NOT create bank_transactions (unmatched rows are reported only — no phantom transactions); single-source / no double-count (file via the existing expenses / rent_payments bank_transaction_id links; lib/statements.ts UNTOUCHED; rail deposits LINK per the Feature-C rule, never double-record); personal/non-rental → excluded; idempotent claims-first + committed-batch no-op; keep parser/matcher/map/plan-builder pure + unit-tested. Do NOT touch lib/statements.ts, lib/reminders.ts, the OFX importer behavior (lib/bank-import/*, import-actions.ts), the reconcile Slice-A action behavior, billing gate keys, vercel.json, or .github/workflows/*.

Process: (1) Deliver the short REVIEW note the ticket's §1 asks for (bank_transactions staging + columns + triage_status values; the rules engine + autoApplyRules; the reconcile Slice-A file/rent/exclude actions incl. rail dedupe; the expense category whitelist; that the accounting gate + manage_work_orders/manage_tenancies capability are the right auth and no billing change is needed), then build. (2) Add scripts/test-accounting-import.ts and run npx tsx on it; run npx tsc --noEmit, npm run lint, npm run build; git diff --check — all green. (3) Write migration 0166 as a file but DO NOT apply it to any database — I apply migrations to prod separately. (4) Commit and push to main; reply with the commit SHA, a file-by-file diffstat, and your review note.
