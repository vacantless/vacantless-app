Vacantless-app — build ticket S521. App HEAD 8d61e27 on main.

Read this file already in the working tree (do NOT ask me to attach anything):
- claude/CODEX-BUILD-QUO-INBOUND-REPLIES-S521.md  — the authoritative build ticket (review, build, verification, go-live).

One-line task: build the QUO/OpenPhone inbound-SMS webhook so renter replies to our shared number are routed, not dropped — honor STOP/START into leads.sms_opt_out, and log conversational replies to the matched org's lead timeline. Concretely — add pure verifyOpenPhoneSignature + parseOpenPhoneInbound to lib/sms.ts (OpenPhone header openphone-signature: hmac;1;<ts>;<b64sig>, HMAC-SHA256 over ts+"."+rawBody with the base64-decoded signing secret, 5-min replay tolerance, constant-time compare; only act on type=message.received + direction=incoming); EXTRACT the existing Twilio route's opt-out logic into a shared lib/sms-inbound.ts applyInboundSms(admin,{from,body}) that both app/api/sms/inbound/route.ts and the NEW app/api/quo/inbound/route.ts call, and EXTEND it so a normal (non-keyword) reply inserts an inbound messages row on each matched lead's timeline (org-scoped); best-effort operator notification via the existing notification substrate if cheap (else note as fast-follow). NO migration (messages + sms_opt_out cols exist).

Follow the ticket exactly. Constraints: ship DARK — with QUO_WEBHOOK_SIGNING_SECRET unset the route is a 200 no-op (zero prod change until I configure the QUO webhook + env); reuse the opt-out logic (extract, don't duplicate — Twilio route must keep working via the shared core); verify signature on every acted request; no app-generated auto-reply; idempotent on OpenPhone retries; no new entitlement/Premium gate; pure helpers have no I/O; do NOT touch the reminder cron, S520 code, or the email path. Note the coordination seam with the dark S518 text-in capture ticket (which also wants app/api/quo/inbound) — build so capture can extend this route later, don't collide.

Process: (1) Deliver the short REVIEW note the ticket asks for (the existing Twilio opt-out logic to reuse; the dark-safe webhook precedent in app/api/inbound/asset; whether a "renter replied" notification event exists/is cheap; the S518 route coordination), then build. (2) Add/extend the tsx test (scripts/test-sms-inbound.ts or test-sms.ts): signature valid/tampered/wrong-key/stale/malformed, parse message.received-incoming vs delivered/call (→null), STOP vs normal-reply routing decision; run npx tsx; run npx tsc --noEmit, npm run lint, npm run build; git diff --check — all green. (3) NO migration. (4) Commit and push to main; reply with the commit SHA, a file-by-file diffstat, and your review note.
